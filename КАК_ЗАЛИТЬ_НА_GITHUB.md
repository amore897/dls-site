# 🚀 Как залить сайт на GitHub - Быстрая Инструкция

## ⚡ Самый Простой Способ - Через Emergent

В интерфейсе Emergent есть кнопка **"Save to Github"** в поле ввода чата:

1. Нажмите на кнопку "Save to Github"
2. Авторизуйтесь через GitHub (если ещё не подключён)
3. Выберите репозиторий (новый или существующий)
4. Готово! ✅

**Emergent автоматически загрузит все файлы правильно!**

---

## 🛠 Ручной Способ (если нужно больше контроля)

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите **"New repository"** (зелёная кнопка)
3. Заполните:
   - Название: `dls-website`
   - Описание: `Digital Labeling System - Official Website`
   - Public или Private (на ваш выбор)
4. **НЕ ставьте галочки** на README, .gitignore, license
5. Нажмите **"Create repository"**

### Шаг 2: Скопируйте URL репозитория

GitHub покажет URL вроде:
```
https://github.com/ваш-username/dls-website.git
```

### Шаг 3: Выполните команды

```bash
cd /app

# 1. Создайте .gitignore (чтобы не загрузить лишнее)
cat > .gitignore << 'EOF'
node_modules/
__pycache__/
.env
*.log
.DS_Store
frontend/build/
.emergent/
EOF

# 2. Инициализируйте Git
git init
git config user.name "Ваше Имя"
git config user.email "ваш-email@example.com"

# 3. Добавьте файлы
git add .
git commit -m "Initial commit: DLS website"

# 4. Подключите GitHub (замените URL на ваш)
git remote add origin https://github.com/ваш-username/dls-website.git
git branch -M main

# 5. Загрузите на GitHub
git push -u origin main
```

### Шаг 4: Авторизация

GitHub попросит логин и пароль:

**ВАЖНО:** Вместо пароля используйте Personal Access Token!

#### Как получить Token:

1. GitHub → **Settings** (справа вверху)
2. **Developer settings** (внизу слева)
3. **Personal access tokens** → **Tokens (classic)**
4. **Generate new token** → **Generate new token (classic)**
5. Выберите срок и поставьте галочку **`repo`**
6. Скопируйте созданный token
7. **При запросе пароля введите этот token**

---

## 🔒 Безопасность - ОЧЕНЬ ВАЖНО!

### Перед загрузкой проверьте:

1. **Файл `.gitignore` создан** ✅
2. **`.env` файлы НЕ загружаются** ✅
3. **Stripe ключи в коде удалены** ✅

### Проверка безопасности:

```bash
# Посмотрите, что будет загружено
git status

# Если увидели .env в списке - удалите его:
git rm --cached backend/.env
git rm --cached frontend/.env
```

**НИКОГДА не загружайте:**
- ❌ `.env` файлы
- ❌ API ключи
- ❌ Пароли
- ❌ Секретные токены

---

## 🔄 Как обновить код после изменений

```bash
cd /app

# Посмотреть что изменилось
git status

# Добавить изменения
git add .

# Сохранить с комментарием
git commit -m "Обновление дизайна"

# Загрузить на GitHub
git push
```

---

## 📱 GitHub Desktop (Для тех, кто не любит командную строку)

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Установите и войдите
3. **File → Add Local Repository**
4. Выберите папку `/app`
5. **Publish repository**
6. Готово!

---

## 🆘 Частые проблемы

### "Permission denied"
→ Используйте Personal Access Token вместо пароля

### "Repository not found"
→ Проверьте правильность URL и название репозитория

### ".env загрузился на GitHub"
```bash
# Удалите из GitHub:
git rm --cached backend/.env
git commit -m "Remove .env"
git push

# Смените Stripe ключи в Stripe Dashboard!
```

---

## 📚 Подробная Инструкция

Полная инструкция со всеми деталями: `/app/GITHUB_DEPLOY.md`

---

**Готово! Ваш код на GitHub! 🎉**

После загрузки вы можете:
- Делиться ссылкой на репозиторий
- Работать в команде
- Отслеживать изменения
- Создавать резервные копии
- Деплоить на хостинг (Vercel, Netlify, etc.)
